# Root .env — consumed by the Expo dashboard (apps/dashboard).
# The backend reads from a separate file: services/backend/.dev.vars
# Copy this to .env and fill in your values.

# URL of the running Hono backend.
# Default: wrangler dev starts on port 8787.
EXPO_PUBLIC_API_URL=http://localhost:8787
